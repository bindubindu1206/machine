# machine
Spam Detection System using Machine Learning  This project is a machine learning-based application that classifies messages (SMS/Email) as Spam or Not Spam using Natural Language Processing (NLP) techniques. The model is trained on labeled text data and deployed using a simple Streamlit web interface for real-time prediction.
